const mongoose = require('mongoose');

// Paste your connection string here (with the autogenerated password)
const uri = 'mongodb+srv://testuser:BqS9UuqhjKnjSppc@cluster0.fnbr4ju.mongodb.net/?appName=Cluster0';

mongoose.connect(uri)
  .then(() => {
    console.log('✅ SUCCESS! MongoDB Connected');
    process.exit(0);
  })
  .catch(err => {
    console.error('❌ Failed:', err.message);
    process.exit(1);
  });